version: v1

health:
  port: 10003
  host: 0.0.0.0
  path: /healtz
  blockchains:
    - blockchain: ethereum
      min-available: 2
    # use `chain` for backward compatibility
    - chain: bitcoin
      min-available: 1