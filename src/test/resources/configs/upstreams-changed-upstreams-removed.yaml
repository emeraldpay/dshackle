cluster:
  upstreams:
    - id: local3
      chain: polygon
      labels:
        provider: provider1
      connection:
        ethereum-pos:
          execution:
            rpc:
              url: "http://localhost"
            ws:
              url: "ws://localhost"