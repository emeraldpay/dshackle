version: v1

upstreams:
  - blockchain: ethereum
    connection:
      ethereum:
        rpc:
          url: "http://localhost:8545"
  - blockchain: ethereum
    id: test
    connection:
      ethereum:
        rpc:
          url: "http://localhost:8545"
  - chain: ethereum # `chain` for backward compatibility
    id: test/test
    connection:
      ethereum:
        rpc:
          url: "http://localhost:8545"
  - chain: ethereum # `chain` for backward compatibility
    id: !test
    connection:
      ethereum:
        rpc:
          url: "http://localhost:8545"
