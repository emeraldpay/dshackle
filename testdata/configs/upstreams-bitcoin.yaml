version: v1

defaults:
  - blockchains:
      - bitcoin
    min-peers: 3

upstreams:
  - id: local
    blockchain: bitcoin
    connection:
      bitcoin:
        rpc:
          url: "http://localhost:8545"